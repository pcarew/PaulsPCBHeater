#include "Arduino.h"
#include "pos.h"

extern void testStart(int);
/*
void setup() {
	Serial.begin(115200);
	Serial.println(F("\n\nIn Setup"));

	testStart(7);

	Serial.println(F("\n\nSetup Complete"));
	if(!start_pos(TRUE)){	// Start POS and allow loop to be a task
		Serial.println(F("Unable to start POS"));
		while(1);
	}
}

void loop() {
	Serial.println(F("Loop doing work"));
	delay(100);
	pause();
}

*/
